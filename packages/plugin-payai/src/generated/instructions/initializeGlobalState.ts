/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/web3.js';
import { PAYAI_MARKETPLACE_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const INITIALIZE_GLOBAL_STATE_DISCRIMINATOR = new Uint8Array([
  232, 254, 209, 244, 123, 89, 154, 207,
]);

export function getInitializeGlobalStateDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_GLOBAL_STATE_DISCRIMINATOR
  );
}

export type InitializeGlobalStateInstruction<
  TProgram extends string = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountSigner extends string | IAccountMeta<string> = string,
  TAccountGlobalState extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            IAccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountGlobalState extends string
        ? WritableAccount<TAccountGlobalState>
        : TAccountGlobalState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeGlobalStateInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeGlobalStateInstructionDataArgs = {};

export function getInitializeGlobalStateInstructionDataEncoder(): Encoder<InitializeGlobalStateInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_GLOBAL_STATE_DISCRIMINATOR,
    })
  );
}

export function getInitializeGlobalStateInstructionDataDecoder(): Decoder<InitializeGlobalStateInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeGlobalStateInstructionDataCodec(): Codec<
  InitializeGlobalStateInstructionDataArgs,
  InitializeGlobalStateInstructionData
> {
  return combineCodec(
    getInitializeGlobalStateInstructionDataEncoder(),
    getInitializeGlobalStateInstructionDataDecoder()
  );
}

export type InitializeGlobalStateAsyncInput<
  TAccountSigner extends string = string,
  TAccountGlobalState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  globalState?: Address<TAccountGlobalState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeGlobalStateInstructionAsync<
  TAccountSigner extends string,
  TAccountGlobalState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: InitializeGlobalStateAsyncInput<
    TAccountSigner,
    TAccountGlobalState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  InitializeGlobalStateInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountGlobalState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYAI_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    globalState: { value: input.globalState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.globalState.value) {
    accounts.globalState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            103, 108, 111, 98, 97, 108, 95, 115, 116, 97, 116, 101,
          ])
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            183, 21, 201, 39, 132, 92, 173, 133, 20, 49, 110, 146, 116, 34, 193,
            22, 230, 35, 4, 182, 110, 1, 127, 84, 6, 28, 105, 208, 196, 28, 74,
            185,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.globalState),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitializeGlobalStateInstructionDataEncoder().encode({}),
  } as InitializeGlobalStateInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountGlobalState,
    TAccountSystemProgram
  >;

  return instruction;
}

export type InitializeGlobalStateInput<
  TAccountSigner extends string = string,
  TAccountGlobalState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  globalState: Address<TAccountGlobalState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeGlobalStateInstruction<
  TAccountSigner extends string,
  TAccountGlobalState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: InitializeGlobalStateInput<
    TAccountSigner,
    TAccountGlobalState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): InitializeGlobalStateInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountGlobalState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYAI_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    globalState: { value: input.globalState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.globalState),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getInitializeGlobalStateInstructionDataEncoder().encode({}),
  } as InitializeGlobalStateInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountGlobalState,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedInitializeGlobalStateInstruction<
  TProgram extends string = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    globalState: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeGlobalStateInstructionData;
};

export function parseInitializeGlobalStateInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedInitializeGlobalStateInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      globalState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeGlobalStateInstructionDataDecoder().decode(
      instruction.data
    ),
  };
}
