/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type IAccountMeta,
  type IAccountSignerMeta,
  type IInstruction,
  type IInstructionWithAccounts,
  type IInstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from '@solana/web3.js';
import { PAYAI_MARKETPLACE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const REFUND_BUYER_DISCRIMINATOR = new Uint8Array([
  199, 139, 203, 146, 192, 150, 53, 218,
]);

export function getRefundBuyerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    REFUND_BUYER_DISCRIMINATOR
  );
}

export type RefundBuyerInstruction<
  TProgram extends string = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountSigner extends string | IAccountMeta<string> = string,
  TAccountContract extends string | IAccountMeta<string> = string,
  TAccountEscrowVault extends string | IAccountMeta<string> = string,
  TAccountBuyer extends string | IAccountMeta<string> = string,
  TAccountGlobalState extends string | IAccountMeta<string> = string,
  TAccountSystemProgram extends
    | string
    | IAccountMeta<string> = '11111111111111111111111111111111',
  TRemainingAccounts extends readonly IAccountMeta<string>[] = [],
> = IInstruction<TProgram> &
  IInstructionWithData<Uint8Array> &
  IInstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            IAccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountContract extends string
        ? WritableAccount<TAccountContract>
        : TAccountContract,
      TAccountEscrowVault extends string
        ? WritableAccount<TAccountEscrowVault>
        : TAccountEscrowVault,
      TAccountBuyer extends string
        ? WritableAccount<TAccountBuyer>
        : TAccountBuyer,
      TAccountGlobalState extends string
        ? WritableAccount<TAccountGlobalState>
        : TAccountGlobalState,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RefundBuyerInstructionData = { discriminator: ReadonlyUint8Array };

export type RefundBuyerInstructionDataArgs = {};

export function getRefundBuyerInstructionDataEncoder(): Encoder<RefundBuyerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REFUND_BUYER_DISCRIMINATOR })
  );
}

export function getRefundBuyerInstructionDataDecoder(): Decoder<RefundBuyerInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRefundBuyerInstructionDataCodec(): Codec<
  RefundBuyerInstructionDataArgs,
  RefundBuyerInstructionData
> {
  return combineCodec(
    getRefundBuyerInstructionDataEncoder(),
    getRefundBuyerInstructionDataDecoder()
  );
}

export type RefundBuyerAsyncInput<
  TAccountSigner extends string = string,
  TAccountContract extends string = string,
  TAccountEscrowVault extends string = string,
  TAccountBuyer extends string = string,
  TAccountGlobalState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  contract: Address<TAccountContract>;
  escrowVault?: Address<TAccountEscrowVault>;
  buyer: Address<TAccountBuyer>;
  globalState?: Address<TAccountGlobalState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRefundBuyerInstructionAsync<
  TAccountSigner extends string,
  TAccountContract extends string,
  TAccountEscrowVault extends string,
  TAccountBuyer extends string,
  TAccountGlobalState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: RefundBuyerAsyncInput<
    TAccountSigner,
    TAccountContract,
    TAccountEscrowVault,
    TAccountBuyer,
    TAccountGlobalState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  RefundBuyerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountContract,
    TAccountEscrowVault,
    TAccountBuyer,
    TAccountGlobalState,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYAI_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    contract: { value: input.contract ?? null, isWritable: true },
    escrowVault: { value: input.escrowVault ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    globalState: { value: input.globalState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrowVault.value) {
    accounts.escrowVault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            101, 115, 99, 114, 111, 119, 95, 118, 97, 117, 108, 116,
          ])
        ),
        getAddressEncoder().encode(expectAddress(accounts.contract.value)),
      ],
    });
  }
  if (!accounts.globalState.value) {
    accounts.globalState.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(
          new Uint8Array([
            103, 108, 111, 98, 97, 108, 95, 115, 116, 97, 116, 101,
          ])
        ),
        getBytesEncoder().encode(
          new Uint8Array([
            183, 21, 201, 39, 132, 92, 173, 133, 20, 49, 110, 146, 116, 34, 193,
            22, 230, 35, 4, 182, 110, 1, 127, 84, 6, 28, 105, 208, 196, 28, 74,
            185,
          ])
        ),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.contract),
      getAccountMeta(accounts.escrowVault),
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.globalState),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getRefundBuyerInstructionDataEncoder().encode({}),
  } as RefundBuyerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountContract,
    TAccountEscrowVault,
    TAccountBuyer,
    TAccountGlobalState,
    TAccountSystemProgram
  >;

  return instruction;
}

export type RefundBuyerInput<
  TAccountSigner extends string = string,
  TAccountContract extends string = string,
  TAccountEscrowVault extends string = string,
  TAccountBuyer extends string = string,
  TAccountGlobalState extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  contract: Address<TAccountContract>;
  escrowVault: Address<TAccountEscrowVault>;
  buyer: Address<TAccountBuyer>;
  globalState: Address<TAccountGlobalState>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRefundBuyerInstruction<
  TAccountSigner extends string,
  TAccountContract extends string,
  TAccountEscrowVault extends string,
  TAccountBuyer extends string,
  TAccountGlobalState extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
>(
  input: RefundBuyerInput<
    TAccountSigner,
    TAccountContract,
    TAccountEscrowVault,
    TAccountBuyer,
    TAccountGlobalState,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress }
): RefundBuyerInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountContract,
  TAccountEscrowVault,
  TAccountBuyer,
  TAccountGlobalState,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? PAYAI_MARKETPLACE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    contract: { value: input.contract ?? null, isWritable: true },
    escrowVault: { value: input.escrowVault ?? null, isWritable: true },
    buyer: { value: input.buyer ?? null, isWritable: true },
    globalState: { value: input.globalState ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  const instruction = {
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.contract),
      getAccountMeta(accounts.escrowVault),
      getAccountMeta(accounts.buyer),
      getAccountMeta(accounts.globalState),
      getAccountMeta(accounts.systemProgram),
    ],
    programAddress,
    data: getRefundBuyerInstructionDataEncoder().encode({}),
  } as RefundBuyerInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountContract,
    TAccountEscrowVault,
    TAccountBuyer,
    TAccountGlobalState,
    TAccountSystemProgram
  >;

  return instruction;
}

export type ParsedRefundBuyerInstruction<
  TProgram extends string = typeof PAYAI_MARKETPLACE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly IAccountMeta[] = readonly IAccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    contract: TAccountMetas[1];
    escrowVault: TAccountMetas[2];
    buyer: TAccountMetas[3];
    globalState: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: RefundBuyerInstructionData;
};

export function parseRefundBuyerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly IAccountMeta[],
>(
  instruction: IInstruction<TProgram> &
    IInstructionWithAccounts<TAccountMetas> &
    IInstructionWithData<Uint8Array>
): ParsedRefundBuyerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = instruction.accounts![accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      contract: getNextAccount(),
      escrowVault: getNextAccount(),
      buyer: getNextAccount(),
      globalState: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRefundBuyerInstructionDataDecoder().decode(instruction.data),
  };
}
